@model IEnumerable<ChatterBoxWebApi.Models.Conversation>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.User_1)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.User_2)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User_1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User_2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConversationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConversationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConversationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
